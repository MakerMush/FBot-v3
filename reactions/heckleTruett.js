module.exports = {
	name: 'heckleTruett',
	execute(message) {
		const reactEmojis = [];
		if (message.member.user.id == 1026599284075921419) {
			reactEmojis.push('😐');
		}

		// return reactEmojis;
	},
};